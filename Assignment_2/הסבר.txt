מטלה 2 - Regression Flow
מצ"ב מטלה 2, אותה יש להגיש בעוד כשבועיים (לפי התאריך שנמצא במטלה).

שימו לב, בזמן ההגשה, ניתן להתעלם מההודעה הבאה (אם תקבלו אותה), מדובר ב-warning ולא בשגיאה:

The file you have submitted has been uploaded but cannot be checked by the teacher.The files that will be able to be checked with the teacher are: doc, docx, pdf, ttp, ttpx, xls, xlsx, rtf, ppt, jpeg, jpg, png.

שימו לב, לגבי ניקוד המטלה, נא לקרוא את ההסבר בהודעה שנשלחה במודל בנושא דגשים לניקוד מטלות התכנות 
בנוסף יש לקרוא את מצגות ההנחיות לגבי המטלות במודל

הקובץ שצריך להגיש:
צריך לעבור על מחברת המטלה ולא לשנות את השם שלה, לבדוק (לפי סדר תאי הקוד) ולשמור את המחברת.
צריך להגיש רק את קובץ מחברת המטלה: Assignment2.ipynb

המטלה מורכבת מפונקציות אותם יש לממש, הכוללות:
1. פרטי סטודנט/ית - שם ותעודת זהות - שימו לב, ישנה חשיבות גדולה למימוש הפונקציות הללו ישר בתחילת המטלה, באופן שמס' תעודת הזהות יהיה תואם לזה שנמצא במודל, זה משפיע על כל המטלה וללא מימושם הבדיקות והמשימות במטלה יפגעו, עד כדי פסילת המטלה. לגבי שם הסטודנט/ית, נא לממש שם באנגלית.
2. טעינת ה-dataset מ-CSV והתאמתו ל-Linear Regression, כולל הוספת מאפיין דמה וחלוקה ל-X (ה-feature vectors) ו-y (ה-target values)
* fictive_attr_name - שם מאפיין הדמה, אותו יש להוסיף נתון כפרמטר לפונקציה.
* הערך של תאי מאפיין הדמה צריכים להיות 1 לכל ה-feature vectors.
* יש להוסיף את המאפיין הפקטיבי, לפני המאפיין הראשון.
* target_column - שם העמודה בה נמצאים ה-target values, גם נתון כפרמטר.
3. scaling  - נא לשים לב לסוג ה-scaling שאותו אתם צריכים לממש (תלוי סטודנט/ית).
* הפעם עושים scaling גם ל-target;
* לא עושים scaling ל-x_0 אבל משאירים אותו
* הפעם מי שעושה minmax, צריך לעשות סילום לתווך בין -1 (מינוס 1) ל1 (כולל) 
4.  predict - הפונקציה מכפילה את וקטור המשקולות w, בכל אחד מה-feature vectors, ומחזירה ב-series  את הערכים של הפרדקציה (בהתאמה ל-dataframe של הקלט ולאינדקסים שלו) 
5. fit - אימון מודל הסיווג של רגרסיה לינארית, המשתמש באלגוריתם gradient descent. 
* יש לאתחל את וקטור w, כוקטור 0 (כלומר כל המשקולות מקבלים ערך התחלתי של 0).
* שימו לב - alpha - הינו קבוע הלמידה;
* num_iterations - מספר האיטרציות. יש להפסיק את הפונקציה ולהחזיר את ה-series של וקטור המשקולות w, בסוף איטרציה מספר num_iterations.
    -  בכל איטרציה - עוברים על כל דוגמאות ה-training.
6. שערוך המודל - מחולק ל-2 פונקציות:
א. פונקצית שיערוך המודל אותה תצטרכו לממש (תלוי סטודנט/ית);
ב. בנוסף צריך לממש את מדד r^2